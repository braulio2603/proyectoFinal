package rentar;

import coleccionClientes.arrayListClientes;
import coleccionClientes.clientes;

import java.util.ArrayList;
import java.util.Scanner;

 /*public class metodoRentar {
    arrayListClientes arraCli = new arrayListClientes();
    ArrayList<clientes> listClientes = arraCli.operacionRentar();
    public void operacionRentar() {

        Scanner scanner = new Scanner(System.in);
        System.out.println("Ingrese su nombre para poder rentar: ");
        String nomCliente = scanner.nextLine();

        clientes busqueda = new clientes(nomCliente, nomCliente);
        boolean existe = false;

        for (int i = 0; i < listaClientes.size(); i++) {
            clientes c = listaClientes.get(i);

            if (c.getNombre().equals(busqueda.getNombre())) {
                existe = true;
                break;
            }
        }
        if (existe) {
            System.out.println("El cliente es correcto");

        } else {
            System.out.println("El cliente es incorrecto");
        }
    }
}
*/
